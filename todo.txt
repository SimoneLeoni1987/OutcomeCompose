[ ] Create an id for every model (to recognise the object) -> Stable id and equals
[ ] How to comunicate the element selected? The entire object? Or just the stable id? -> Every object that
    has a selection inside, should have a bubble up method
[ ] Ui refactoring, using the right UI colors and styling
[ ] Create mocking class methods
[ ] Create the equals for every model (to recognise any change in the object) -> All the content,
    even the one that can change
[ ] Create the linking between the betslip, to notify the click and to get the updated state on the
    outcome -> BetslipRepository injected in the outcome object and listening to the flow